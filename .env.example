# Copy to .env.local to point the front end at a real backend.
# Leave unset (or empty) to run in local mock mode (localStorage, no server).
#
# Example:
# VITE_API_BASE=https://api.mrben.com
VITE_API_BASE=
